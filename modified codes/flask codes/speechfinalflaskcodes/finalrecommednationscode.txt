<!-- templates/recommendations.html -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recommendations</title>
</head>
<body>
    <h1>Recommended Dishes:</h1>
    {% if recommendations %}
        <ul>
            {% for dish in recommendations %}
                <li>{{ dish[0] }} - {{ dish[1] }} - {{ dish[2] }}</li>
            {% endfor %}
        </ul>
    {% else %}
        <p>No recommended dishes found based on your preferences.</p>
    {% endif %}

    <!-- Added audio player for recommendations -->
    <audio controls>
        <source src="{{ url_for('static', filename='temp.mp3') }}" type="audio/mp3">
        Your browser does not support the audio element.
    </audio>

    <!-- Language selection dropdown -->
    <form method="post">
        <label for="language">Select Language:</label>
        <select name="language" id="language">
            {% for lang in languages %}
                <option value="{{ lang }}" {% if lang == 'en' %}selected{% endif %}>{{ lang }}</option>
            {% endfor %}
        </select>
        <button type="submit">Submit</button>
    </form>
</body>
</html>
